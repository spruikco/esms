<!DOCTYPE html>
<html>
<head>
    <title>Simulate Match | ESMS League Manager</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
    <style>
        .back-link {
            margin-bottom: 20px;
            display: inline-block;
        }
        
        .select-roster {
            margin-top: 10px;
            margin-bottom: 15px;
        }
        
        select {
            padding: 8px;
            border-radius: 4px;
            border: 1px solid #ddd;
            width: 100%;
            box-sizing: border-box;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="container">
        <a href="{{ url_for('index') }}" class="back-link">&larr; Back to Menu</a>
        
        <h1>Simulate Match</h1>
        <p>Select team files and upload teamsheets to run a match simulation.</p>
        
        <form action="{{ url_for('simulate') }}" method="post" enctype="multipart/form-data">
            <div class="team-section">
                <h2>Home Team</h2>
                
                <div class="form-group">
                    <label for="home_roster">Roster:</label>
                    <div class="select-roster">
                        <select name="home_roster_select" id="home_roster_select">
                            <option value="">Upload a file instead</option>
                            {% for roster in rosters %}
                                <option value="{{ roster }}">{{ roster }}</option>
                            {% endfor %}
                        </select>
                    </div>
                    <input type="file" name="home_roster" id="home_roster">
                    <small>Select from available rosters or upload a new one</small>
                </div>
                
                <div class="form-group">
                    <label for="home_teamsheet">Teamsheet:</label>
                    <input type="file" name="home_teamsheet" id="home_teamsheet" required>
                    <small>Upload the home team's teamsheet file</small>
                </div>
            </div>
            
            <div class="team-section">
                <h2>Away Team</h2>
                
                <div class="form-group">
                    <label for="away_roster">Roster:</label>
                    <div class="select-roster">
                        <select name="away_roster_select" id="away_roster_select">
                            <option value="">Upload a file instead</option>
                            {% for roster in rosters %}
                                <option value="{{ roster }}">{{ roster }}</option>
                            {% endfor %}
                        </select>
                    </div>
                    <input type="file" name="away_roster" id="away_roster">
                    <small>Select from available rosters or upload a new one</small>
                </div>
                
                <div class="form-group">
                    <label for="away_teamsheet">Teamsheet:</label>
                    <input type="file" name="away_teamsheet" id="away_teamsheet" required>
                    <small>Upload the away team's teamsheet file</small>
                </div>
            </div>
            
            <div class="form-actions">
                <button type="submit" class="btn">Simulate Match</button>
            </div>
        </form>
        
        <div class="sample-section">
            <h2>Don't have team files?</h2>
            <p>Try a sample match with pre-loaded teams.</p>
            <a href="{{ url_for('sample') }}" class="btn sample-btn">Run Sample Match</a>
        </div>
    </div>

    <script>
        // JavaScript to handle roster selection
        document.getElementById('home_roster_select').addEventListener('change', function() {
            var fileInput = document.getElementById('home_roster');
            if (this.value) {
                fileInput.disabled = true;
                fileInput.required = false;
            } else {
                fileInput.disabled = false;
                fileInput.required = true;
            }
        });

        document.getElementById('away_roster_select').addEventListener('change', function() {
            var fileInput = document.getElementById('away_roster');
            if (this.value) {
                fileInput.disabled = true;
                fileInput.required = false;
            } else {
                fileInput.disabled = false;
                fileInput.required = true;
            }
        });
    </script>
</body>
</html>